version: '3.9'
services:
  api:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ../../:/app
    command: sh -c "pnpm install && pnpm --filter @cashtagalert/api dev"
    ports:
      - "4000:4000"
